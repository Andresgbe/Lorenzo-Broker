export { VideoPlayer } from './ui/VideoPlayer';
export type { VideoSource } from './model/useVideoSource';
// Note: useVideoSource not exported as per explicit instructions to ONLY export VideoPlayer and VideoSource
